apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: devguard-k8s-image-inventory
  name: devguard-k8s-image-inventory
  namespace: devguard
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: devguard-k8s-image-inventory
  template:
    metadata:
      labels:
        app.kubernetes.io/name: devguard-k8s-image-inventory
    spec:
      containers:
      - image: ghcr.io/l3montree-dev/devguard-k8s-image-inventory:main-amd64
        name: operator
        env:
          - name: DEVGUARD_TOKEN
            valueFrom:
              secretKeyRef:
                name: "devguard-k8s-image-inventory"
                key: "devguard-token"
          - name: POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: DEVGUARD_PROJECT_URL
            value: "https://api.main.devguard.org/api/v1/organizations/l3montree-cybersecurity/projects/cluster/external"
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        securityContext:
          capabilities:
            drop:
            - ALL
          privileged: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 101
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
          - mountPath: /work
            name: work
          - mountPath: /tmp
            name: tmp
        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 100Mi
        livenessProbe:
          timeoutSeconds: 3
          httpGet:
            path: "/health"
            port: 8081
        readinessProbe:
          timeoutSeconds: 3
          httpGet:
            path: "/health"
            port: 8081
      volumes:
        - name: work
          emptyDir: {}
        - name: tmp
          emptyDir: {}
      securityContext:
        fsGroup: 101
      serviceAccountName: devguard-k8s-image-inventory
